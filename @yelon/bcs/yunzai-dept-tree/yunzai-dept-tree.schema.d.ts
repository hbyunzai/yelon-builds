import { Observable } from 'rxjs';
import { SFSchema, SFSchemaEnumType } from '@yelon/form';
export declare function generateSchema(ic: boolean, ich: boolean, gra: boolean, data?: Observable<SFSchemaEnumType[]>): SFSchema;
